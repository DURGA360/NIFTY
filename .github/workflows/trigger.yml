name: Trigger Python Script

on:
  workflow_dispatch:
    inputs:
      custom_event_name:
        description: 'dj'
        required: true

jobs:
  trigger_script:
    runs-on: ubuntu-latest

    steps:
    - name: nifty.py
      run: echo "nifty"
